{{# with leave ~}}
  {{~# if this.get_leave_type_name ~}}{{~ this.get_leave_type_name }} {{/if~}}
  από {{# with this.get_start_leave_day ~}}
    {{~ as_date this.date ~}}
    {{~# if this.is_morning_leave}} (πρωί){{/if~}}
    {{~# if this.is_afternoon_leave}} (απόγευμα){{/if~}}
  {{~/with}} μέχρι {{# with this.get_end_leave_day ~}}
    {{~ as_date this.date ~}}
    {{~# if this.is_morning_leave}} (πρωί){{/if~}}
    {{~# if this.is_afternoon_leave}} (απόγευμα){{/if~}}
  {{~/with~}}
{{~/with~}}
